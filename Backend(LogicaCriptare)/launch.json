{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Run ts index.ts",
      "program": "${workspaceFolder}/index.ts",
      // "outFiles": ["${workspaceFolder}/**/*.js"],
      "preLaunchTask": "tsc: build - tsconfig.json"
    }
  ]
}